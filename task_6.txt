Запуск побайтового бесконечного копирования нулей:

dd if=/dev/zero of=/dev/zero

Запуская команду kill и направляя разные типы сигналов происходит завершение процесса с последующим сообщением, в зависимости от типа сигнала.