Создаём несколько файлов:

touch file{1..4}.txt


Cоздаём директорию, перемещаем файл в неё:

mkdir newdir
mv file[1-4].txt ./newdir


Удаление всех созданных директорий и файлов:

rm -r *